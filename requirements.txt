# Research Agent Harness — dependencies
# Install: pip install -r requirements.txt
# Then install the harness itself: pip install -e .
#
# Core dependency is OpenProgram (the agentic programming framework).
# Install it first:
#   pip install git+https://github.com/Fzkuji/OpenProgram.git
# Or clone locally and pip install -e <path>.

openprogram @ git+https://github.com/Fzkuji/OpenProgram.git

# Optional: PDF parsing (for reading/reviewing paper PDFs)
# pip install pymupdf
# PyMuPDF

# Optional: Wiki integration
# pip install git+https://github.com/Fzkuji/Wiki-Agent-Harness.git

# Optional: Web search / literature retrieval
requests>=2.31
